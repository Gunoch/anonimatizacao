pymupdf>=1.25.0
spacy>=3.6.0
faker>=18.0.0
transformers>=4.30.0
tokenizers>=0.13.0
torch>=2.0.0
accelerate>=1.7.0
tqdm>=4.65.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0

# For spaCy Portuguese model (install separately):
# python -m spacy download pt_core_news_sm

# For OCR support (optional):
# pytesseract>=0.3.10
# Pillow>=10.0.0

# For packaging (install separately if needed):
# pyinstaller>=5.10.0

# For testing (included above):
# faker - for generating synthetic test data
# pandas - for test results analysis
# matplotlib - for test report visualization
